name: Deploy to Raspberry Pi

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Deploy to Raspberry Pi
      uses: appleboy/ssh-action@v0.1.10
      with:
        host: "2804:d57:4e2e:ee00:e65f:1ff:fe06:e765"
        username: ${{ secrets.RASPBERRY_PI_USER }}
        key: ${{ secrets.RASPBERRY_PI_SSH_KEY }}
        port: 22
        timeout: 120s
        script: |
          set -e
          echo "🚀 Starting deployment..."
          
          DEPLOY_DIR="/home/ulisses/apps/comparador-precos"
          cd $DEPLOY_DIR
          
          echo "📦 Pulling latest code..."
          git pull origin main
          
          echo "🐳 Starting fast deploy..."
          chmod +x deploy-fast.sh
          ./deploy-fast.sh
          
          echo "✅ Deploy completed successfully!"